# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.eggs/
dist/
build/
.venv/

# ── 中间产物（Pipeline 生成，不入库） ──
data/parsed/
data/filtered.csv
data/kb_internal.csv
data/changelog.csv
data/*.tmp

# ── 最终产出（太大，通常不入库） ──
merchant_kb.csv
merchant_kb_cleaned.csv
merchant_kb_cleaning_report.csv

# ── 原始报文（太大，不入库） ──
xml_input/*.xml

# ── 备份（旧文件存档） ──
backup/
docs/
historical_kb/
manual_entries/
tests/
# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── 采样数据（太大，不入库）──
sample.csv

# ── 运行时产出（不入库）──
cache/
output/

# ── 临时文件 ──
*.tmp
*.bak
*.log
